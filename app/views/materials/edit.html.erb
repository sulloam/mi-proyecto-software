<h1>Editar Material</h1>

<%= form_with model: [@course, @material], local: true do |form| %>
  <div class="field">
    <%= form.label :title, "Título del Material" %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :embed_url, "Enlace de YouTube (embed)" %>
    <%= form.text_field :embed_url, placeholder: "https://www.youtube.com/embed/..." %>
  </div>

  <div class="actions">
    <%= form.submit "Actualizar Material", class: "btn btn-primary" %>
  </div>
<% end %>

<%= link_to 'Volver', my_courses_courses_path, class: 'btn btn-secondary' %>
